<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lazai.mapper.LazbubuDataWhitelistMapper">
    <sql id="Base_Column_List">
        id, user_id, status, content, source, created_at, updated_at, biz_type, completed_cnt
    </sql>

    <sql id="insertBasic">
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">
                user_id,
            </if>
            <if test="status != null">
                status,
            </if>
            <if test="bizType != null">
                biz_type,
            </if>
            <if test="content != null">
                content,
            </if>
            <if test="source != null">
                source,
            </if>
            <if test="completedCnt != null">
                completed_cnt,
            </if>
            created_at,
            updated_at,
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">
                #{userId},
            </if>
            <if test="status != null">
                #{status},
            </if>
            <if test="bizType != null">
                #{bizType},
            </if>
            <if test="content != null">
                #{content},
            </if>
            <if test="source != null">
                #{source},
            </if>
            <if test="completedCnt != null">
                #{completedCnt},
            </if>
            #{createdAt},
            #{updatedAt},
        </trim>
    </sql>



    <sql id="updateBasic">
        <set>
            <if test="status != null">
                status=#{status},
            </if>
            <if test="bizType != null">
                biz_type=#{bizType},
            </if>
            <if test="content != null">
                content=#{content},
            </if>
            <if test="completedCnt != null">
                completed_cnt=#{completedCnt},
            </if>
            updated_at=#{updatedAt},
        </set>
    </sql>

    <insert id="insert"  useGeneratedKeys="true" keyProperty="id" parameterType="com.lazai.entity.LazbubuDataWhitelist">
        insert into lazbubu_data_whitelist
        <include refid="insertBasic"/>
    </insert>

    <update id="updateById" parameterType="com.lazai.entity.LazbubuDataWhitelist">
        update lazbubu_data_whitelist
        <include refid="updateBasic"/>
        where id = #{id}
    </update>

    <select id="queryList" resultType="com.lazai.entity.LazbubuDataWhitelist">
        SELECT
        <include refid="Base_Column_List"/>
        from lazbubu_data_whitelist
        <where>

            <if test="userIds != null">
                AND user_id IN
                <foreach collection="userIds" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <if test="statusList != null">
                AND status IN
                <foreach collection="statusList" item="status" open="(" separator="," close=")">
                    #{status}
                </foreach>
            </if>

            <if test="bizType != null">
                AND biz_type = #{bizType}
            </if>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="source != null">
                AND source = #{source}
            </if>
            <if test="minId != null">
                AND id > #{minId}
            </if>
            <if test="createdStart != null">
                AND created_at &gt;= #{createdStart}
            </if>
            <if test="createdEnd != null">
                AND created_at &lt;= #{createdEnd}
            </if>
        </where>
        ORDER BY id asc
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

    <select id="pageQueryList" resultType="com.lazai.entity.LazbubuDataWhitelist">
        SELECT
        <include refid="Base_Column_List"/>
        from lazbubu_data_whitelist
        <where>

            <if test="userIds != null">
                AND user_id IN
                <foreach collection="userIds" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <if test="statusList != null">
                AND status IN
                <foreach collection="statusList" item="status" open="(" separator="," close=")">
                    #{status}
                </foreach>
            </if>

            <if test="bizType != null">
                AND biz_type = #{bizType}
            </if>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="source != null">
                AND source = #{source}
            </if>
            <if test="minId != null">
                AND id > #{minId}
            </if>
            <if test="createdStart != null">
                AND created_at &gt;= #{createdStart}
            </if>
            <if test="createdEnd != null">
                AND created_at &lt;= #{createdEnd}
            </if>
        </where>
        ORDER BY id desc
        LIMIT #{offset}, #{limit}
    </select>

    <select id="pageQueryCnt" resultType="java.lang.Integer">
        SELECT
        count(id)
        from lazbubu_data_whitelist
        <where>

            <if test="userIds != null">
                AND user_id IN
                <foreach collection="userIds" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
            <if test="statusList != null">
                AND status IN
                <foreach collection="statusList" item="status" open="(" separator="," close=")">
                    #{status}
                </foreach>
            </if>

            <if test="bizType != null">
                AND biz_type = #{bizType}
            </if>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="source != null">
                AND source = #{source}
            </if>
            <if test="minId != null">
                AND id > #{minId}
            </if>
            <if test="createdStart != null">
                AND created_at &gt;= #{createdStart}
            </if>
            <if test="createdEnd != null">
                AND created_at &lt;= #{createdEnd}
            </if>
        </where>
    </select>

</mapper>